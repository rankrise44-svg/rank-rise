# Copy to .env (or set in Netlify → Site settings → Environment variables).
# Without it the app still runs end-to-end on the bundled sample business —
# every screen is marked SAMPLE so a demo never silently passes fiction off
# as a real analysis.
ANTHROPIC_API_KEY=sk-ant-...
